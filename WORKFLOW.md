# 实习代码 Git 工作流

> 一句话版本：**改代码 → VS Code 提交 → 推送**。所有实习工程都在一个仓库里，按文件夹区分。

---

## 1. 本地仓库在哪

```
E:\Shixi\实习成果备份区\        ← 这就是 Git 仓库根目录（本地）
```

远程仓库（GitHub）：`https://github.com/XinD822/MyShixiCode.git`（main 分支）

**日常就用 VS Code 打开这个文件夹**（文件 → 打开文件夹 → 选 `E:\Shixi\实习成果备份区`），左侧会显示源代码管理。

## 2. 目录结构（每个工程一个文件夹）

| 文件夹 | 内容 |
|---|---|
| `f103u盘备份/` | F103 USB 工程 |
| `f407usb与u盘/` | F407 USB 与 U 盘工程 |
| `gd32f407usb与u盘/` | GD32F407 USB 与 U 盘工程 |
| `gd32f407usb与u盘最终版本/` | GD32F407 最终版本（升级 + bootloader） |
| `usb_upgrade_platformf103/` | F103 升级平台 |

> 被 git 忽略、不进仓库的：`- 副本`、`备份2/3` 等手动备份文件夹（磁盘上还留着，但不用再手动备份了——git 本身就是备份）、Keil 构建产物（Objects/Listings/OutPut 等）。

## 3. 每日流程（90% 的时间用 VS Code 就够了）

1. 打开 VS Code，打开 `E:\Shixi\实习成果备份区`
2. 改代码（VS Code + Keil 插件：改完在 VS Code 里写代码，Keil 里编译调试）
3. 左侧「源代码管理」图标 → 看到改动文件 → 输入提交说明 → 点 **✓ 提交**
4. 点「同步更改」（推送图标）推到 GitHub
5. 完成

### 提交说明怎么写

简单写清"做了什么"即可，例如：
- `修改led闪烁频率`
- `修复usb枚举超时`
- `新增fatfs写入功能`

### 命令行版（不想用 VS Code 时）

```bash
cd E:/Shixi/实习成果备份区
git add -A
git commit -m "修改led闪烁频率"
git push
```

## 4. 要做新功能 / 新工程怎么办

**新功能（已有工程内）**：直接在对应文件夹里改代码，按上面的每日流程提交即可。改完一个功能就提交一次，出问题能回退。

**全新工程（新文件夹）**：
1. 在 `E:\Shixi\实习成果备份区` 下新建文件夹（如 `stm32f407_i2c_测试`）
2. 正常用 VS Code/Keil 写代码
3. 提交推送时 git 会自动把这个文件夹加进仓库
4. 如果新工程是 Keil 工程，它的 `Objects/`、`Listings/`、`OutPut/` 等构建产物**已经被根 .gitignore 自动忽略**，不用管

> 提示：`.gitignore` 是全局规则，新 Keil 工程不用自己建，根目录那份已覆盖所有子文件夹。

## 5. 必须记住的 3 个原则

1. **已经推送过的提交不要"修改上一次提交"（amend）** —— 在 VS Code 提交按钮的箭头下拉里，选「提交」而不是「修改上一次提交」。amend 会导致和远程历史分叉，弹出你上次遇到的"变基"提示。
2. **每天下班前推一次** —— git 就是你的自动备份，推送了才真正安全。
3. **看到"变基/合并"提示时先停手** —— 说明历史分叉了，先 `git status` 看 `[ahead X, behind Y]`，两个都不为 0 就找组长/同事或查一下再操作，别乱点。

## 6. 常见问题

**Q: 推送时报 "rejected / non-fast-forward"？**
A: 远程有新提交你没拉到。先 `git pull`（VS Code 里点同步），有冲突就解决冲突，再推送。

**Q: 不小心点了"修改上一次提交"导致变基提示？**
A: 不要继续。用 VS Code 的「源代码管理 → 更多操作 → 放弃」或者直接告诉我，我可以帮你恢复。

**Q: 想找回之前删掉的文件？**
A: git 里能找回。提交历史里搜文件名，`git log --oneline -- <文件名>` 定位后恢复。

**Q: 我改了代码但忘了提交，想看看改了什么？**
A: VS Code 源代码管理面板里点文件名就能看改动（左边原文件/右边新文件）。

**Q: 需要另一个芯片版本（如 gd32 的另一个变体）？**
A: 和 F103/F407 差异大就新建文件夹；差异小（同芯片族小改动）可以先用一个文件夹，需要时再说分支，现阶段用不上分支。

## 7. 万一仓库乱了怎么办

- **先别慌，什么都别删**。git 的提交历史都在，绝大多数问题都能恢复。
- 找我会话/同事帮忙，描述你做了什么操作，不要自己执行 `git reset --hard` / `git clean` 等危险命令。

---

*本文件由 AtomCode 于 2026-08-18 生成，随仓库一起维护。*
